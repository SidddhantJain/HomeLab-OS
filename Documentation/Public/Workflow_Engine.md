# Automation Workflow Engine Architecture

## Overview
Evaluates `IF (conditions) THEN (actions)` rules based on triggers.

## Triggers
- `time`, `event`, `threshold`, `manual`

## Actions
- `backup`, `snapshot`, `cleanup`, `notification`, `vault_lock`, `shutdown`, `restart`
